import React from 'react';
import StartSplash from '../components/StartSplash';

/**
 * Start Splash Page.
 *
 * @returns {React.ReactElement} Splash Page.
 */
const SplashPage = (): React.ReactElement => (
    <StartSplash />
);
export default SplashPage;
